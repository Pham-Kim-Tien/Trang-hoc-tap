<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Trang học tập cho học sinh lớp 5</title>
    <link href="https://fonts.googleapis.com/css2?family=Fredoka+One&display=swap|Trirong" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@400;600;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Baloo+2:wght@400;600;700&display=swap" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@400;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="styles.css">
</head>
<body>

    <!-- Đầu trang -->
    <header>
        <h1>Học Tập Vui Vẻ</h1>
    </header>

    <!-- Menu điều hướng -->
    <nav>
        <ul>
            <li><a href="#" onclick="toggleSection('math')">Toán</a></li>
        </ul>
    </nav>

    <!-- Trang chủ với nền hình ảnh động -->
    <section id="home">
        <div id="background-image"></div>
        </div>
    </section>

    <!-- Các phần nội dung học tập -->
    <section id="math" style="display: none;">
        <h2>Môn Toán - Các dạng toán có lời văn điển hình</h2>
        
        <!-- Mục Dạng Toán -->
        <ul class="week-list">
        <li onclick="showWeekContent(1)">Dạng toán tổng tỉ</li>
        <li onclick="showWeekContent(2)">Dạng toán hiệu tỉ</li>
        </ul>

    <!-- Nội dung từng dạng -->
    <div id="week-content">
        <div id="week-1" class="week-detail" style="display: none;">
            <h4>Dạng toán tổng tỉ</h4>
            <ul>
                <li onclick="showSubContent('week-1-knowledge')"><strong>Kiến Thức Cần Nhớ<strong></li>
                <li><a href="bài tập vận dụng dạng 1.html"><strong>Bài Tập Vận Dụng<strong></a></li>
            </ul>
            <div id="week-1-knowledge" class="sub-detail" style="display: none;">
              <p><strong>Cách giải:<strong></p>
              <div class="steps">
              <div class="step">Bước 1: Vẽ sơ đồ</div>
              <div class="step">Bước 2: Tính tổng số phần bằng nhau</div>
              <div class="step">Bước 3: Tính giá trị một phần</div>
              <div class="step">Bước 4: Số lớn bằng: Giá trị một phần x Số phần số lớn</div>
              <div class="step">Bước 5: Số bé bằng: Tổng - Số lớn</div>
            </div>
        </div>
            <div id="week-1-exercise" class="sub-detail" style="display: none;">
                <h5>Bài Tập Vận Dụng</h5>
            </div>
        </div>

        <div id="week-2" class="week-detail" style="display: none;">
            <h4>Dạng toán hiệu tỉ</h4>
            <ul>
                <li onclick="showSubContent('week-2-knowledge')">Kiến Thức Cần Nhớ</li>
                <li><a href="bài tập vận dụng dạng 2.html">Bài Tập Vận Dụng</a></li>
            </ul>
            <div id="week-2-knowledge" class="sub-detail" style="display: none;">
                <p>Cách giải:</p>
              <div class="steps">
              <div class="step">Bước 1: Vẽ sơ đồ</div>
              <div class="step">Bước 2: Tính hiệu số phần bằng nhau</div>
              <div class="step">Bước 3: Tính giá trị một phần</div>
              <div class="step">Bước 4: Số lớn bằng: Giá trị một phần x Số phần số lớn</div>
              <div class="step">Bước 5: Số bé bằng: Số lớn - Hiệu</div>
            </div>
          </div>
            <div id="week-2-exercise" class="sub-detail" style="display: none;">
                <h5>Bài Tập Vận Dụng</h5>
            </div>
        </div>
        <!-- Thêm các dạng khác tương tự -->
    </section>

    <!-- Chân trang -->
    <footer>
        <p>&copy; 2024 Trang học tập cho học sinh lớp 5 - Tác giả: Phạm Kim Tiến</p>
    </footer>

  <!-- JavaScript để điều khiển nội dung động -->
<script>
    function toggleSection(sectionId) {
        const sections = document.querySelectorAll('section');
        sections.forEach(section => {
            if (section.id === sectionId) {
                section.style.display = section.style.display === 'none' ? 'block' : 'none';
            } else {
                section.style.display = 'none';
            }
        });
    }

    function showWeekContent(weekNumber) {
        // Ẩn danh sách tuần
        document.querySelector('.week-list').style.display = 'none';

        // Ẩn tất cả các nội dung tuần khác
        const allWeekDetails = document.querySelectorAll('.week-detail');
        allWeekDetails.forEach(detail => detail.style.display = 'none');

        // Hiển thị nội dung của dạng toán đã chọn
        const selectedWeek = document.getElementById(`week-${weekNumber}`);
        if (selectedWeek) {
            selectedWeek.style.display = 'block';
        }
    }

    function showSubContent(subContentId) {
        // Ẩn tất cả nội dung con
        const allSubContents = document.querySelectorAll('.sub-detail');
        allSubContents.forEach(content => content.style.display = 'none');

        // Hiển thị nội dung con đã chọn
        const selectedSubContent = document.getElementById(subContentId);
        if (selectedSubContent) {
            selectedSubContent.style.display = 'block';
        }
    }

    function showWeekList() {
        // Hiển thị lại danh sách dạng toán
        document.querySelector('.week-list').style.display = 'block';

        // Ẩn tất cả các nội dung tuần
        const allWeekDetails = document.querySelectorAll('.week-detail');
        allWeekDetails.forEach(detail => detail.style.display = 'none');
    }
</script>
</body>
</html>
